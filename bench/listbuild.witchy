// Allocation-bound: build a 300k-element list, then fold it.
fn main(console: Console):
    var xs = []
    for i in 0..300000:
        xs = list.push(xs, i * 3)
    var total = 0
    for x in xs:
        total = total + x % 7
    print(console, "${total}")

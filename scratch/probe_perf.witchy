// Probe: code that should trigger a perf note about copy paths.
import list

fn build(n: Int) -> List(Int):
    var xs = []
    var i = 0
    while i < n:
        xs = list.push(xs, i)
        i = i + 1
    xs

fn main(console: Console):
    let xs = build(5)
    print(console, "${xs}")

// 01_hello.witchy — the obligatory hello, plus a quick taste of strings,
// interpolation, and a pure helper.
fn greet(name: String) -> String:
    "hello, " <> name <> "!"

fn main(console: Console):
    print(console, greet("witchy"))
    let n = 7
    print(console, "n=${n}, n*n=${n * n}")

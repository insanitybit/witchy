import iter

fn main(console: Console):
    let literal = [1, 2, 3]
    print(console, "literal list: ${literal}")
    let collected = iter.collect(iter.take(iter.range(1, 100), 3))
    print(console, "collected list: ${collected}")

import result

fn f(ok: Bool) -> Result(Int, String):
    if ok:
        Ok(42)
    else:
        Err("boom")

fn main(console: Console):
    match f(false):
        Ok(v) -> print(console, "ok: ${v}")
        Err(e) -> print(console, "err: ${e}")

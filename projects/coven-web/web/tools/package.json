{
  "name": "coven-web-tools",
  "private": true,
  "description": "Vendored, pinned build tools for coven-web. NOT runtime deps — the SPA itself has zero dependencies (see ../package.json). Pinned per PLAN.md WS-D.",
  "version": "0.0.0",
  "dependencies": {
    "esbuild": "0.27.4",
    "typescript": "5.9.3",
    "oxlint": "1.56.0"
  }
}

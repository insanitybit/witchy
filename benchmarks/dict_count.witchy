fn main(console: Console, clock: Clock):
    let bench_t0 = now_monotonic(clock)
    var d = dict.new()
    var i = 0
    while i < 3000000:
        let k = i % 1000
        d = dict.insert(d, k, dict.get_or(d, k, 0) + 1)
        i = i + 1
    var total = 0
    for k in dict.keys(d):
        total = total + dict.get_or(d, k, 0)
    let bench_t1 = now_monotonic(clock)
    print(console, "${total}")
    print(console, "bench_ns=${bench_t1 - bench_t0}")

// A library module with an explicit checked purity contract. Importing it grants
// no root authority, and `shout` cannot exercise delegated authority either.
pub pure fn shout(name: String) -> String:
    "HELLO, " + name

# Dynamic reflection

Demonstrates exact runtime type descriptors, public field inspection,
compiler-authenticated trait checks, and checked invocation of an explicitly
registered `@dynamic` method.

Run it from the repository root:

```sh
cargo run -- run examples/dynamic_reflection
```

actor Printer:
    console: Console

impl Printer:
    on Show(n: Int):
        print(console, "printer got " <> int_to_string(n))

actor Forwarder:
    target: Subject

impl Forwarder:
    on Relay(n: Int):
        send(target, Show(n))

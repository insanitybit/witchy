// `enumerate` pairs each element with its index; `zip` pairs two lists
// element-wise. Both return lists of tuples, destructured in the loop.

import list
import string

fn main(console: Console):
    let names = ["alice", "bob", "carol"]

    var labelled = []
    for pair in names.enumerate():
        let (i, name) = pair
        labelled = labelled.push("${i}" + ":" + name)
    print(console, labelled.join(" "))

    let ages = [30, 25, 40]
    var joined = []
    for entry in names.zip(ages):
        let (name, age) = entry
        joined = joined.push(name + "=" + "${age}")
    print(console, joined.join(" "))

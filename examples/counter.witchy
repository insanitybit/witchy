// Per-actor state: `count` persists across messages, and only Tick can
// change it — no other code can reach an actor's state from outside.
actor Counter:
    console: Console
    var count: Int = 0

impl Counter:
    on Tick():
        count = count + 1
        print(console, "count is " <> int_to_string(count) <> "\n")

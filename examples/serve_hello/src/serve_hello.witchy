// A capability-secure, MULTI-CORE web server. Handlers are pure
// `fn(Request) -> Response` — they have no Net/Dir/Console, so a handler literally
// cannot touch the network, filesystem, or console. Only `main` holds the `Net`, and
// it hands it to `serve` to listen — never to a handler. The router, path params, and
// middleware are axum-flavored; the capability sandbox is pure witchy.
//
// `server.serve` uses ALL cores for free: it runs a prefork pool of worker VMs that
// share the listener, and the kernel load-balances connections across them. You write
// the handlers; the parallelism is automatic. (Use `server.serve_one` for one core.)
// Run: witchy --net 127.0.0.1:8080 examples/serve_hello.witchy

from http import Request, Response
from server import Router

fn home(req: Request) -> Response:
    server.text(200, "hello, witchy")

fn show_user(req: Request) -> Response:
    server.text(200, "user " + server.param_or(req, "id", ""))

fn main(console: Console, net: Net):
    let app = server.router()
        .get("/", home)
        .get("/users/:id", show_user)
        .post("/echo", fn(req: Request): server.text(200, server.request_body(req)))
    print(console, "serving on 127.0.0.1:8080")
    server.serve(net, "127.0.0.1:8080", app)

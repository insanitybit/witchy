// In-rune tests for `ownership`. Each `test_*` function takes no parameters,
// passes by returning, and fails by aborting (the `testing` assertions do that
// with a message). Run them with `witchy test examples/ownership`.

import ownership
import testing

fn test_into_label():
    testing.assert_eq(ownership.into_label("witchy"), "[witchy]")
